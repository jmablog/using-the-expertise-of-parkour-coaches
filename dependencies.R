# use this file to list any other libraries not explicitly called in another
# script for renv to detect and save in renv.lock.
# This can also be achieved by listing them under Imports in the DESCRIPTIONS
# file, which will also allow devtools::install_deps() to work as an alternative
# install method.
library(devtools)
library(todor)
